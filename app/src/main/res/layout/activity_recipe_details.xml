<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RecipeDetails">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/food_recipe"

        android:scaleType="centerCrop"
        android:id="@+id/recipeDetails_imageView_banner"
        />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_baseline_arrow_back_ios_24"
        android:layout_margin="32dp"
        android:padding="10dp"
        android:id="@+id/recipeDetails_imageView_back"
        android:background="@drawable/transparent_round_circle"/>
<include layout="@layout/bottom_sheet_recipe_details"/>
</androidx.coordinatorlayout.widget.CoordinatorLayout>